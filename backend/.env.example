# Shared secret required as a Bearer token on every route except GET /health.
APP_SECRET=change-me

# JSON list of allowed CORS origins for the frontend, e.g. the Vite dev server.
CORS_ORIGINS=["http://localhost:5173"]
